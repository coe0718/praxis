[advisories]
ignore = [
    "RUSTSEC-2024-0436",  # paste unmaintained (via ratatui) — stable, no risk
    "RUSTSEC-2026-0002",  # lru IterMut unsound (via ratatui) — edge case
    "RUSTSEC-2026-0097",  # rand unsound with custom logger (via quinn-proto) — not used
]
